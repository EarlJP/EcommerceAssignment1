@model ClientManager.Models.person

@{
    ViewBag.Title = "Index";
}

<h2>
    @Html.DisplayFor(model => model.first_name)
    @Html.DisplayFor(model => model.last_name)
</h2>

<p>
    @Html.ActionLink("Create New", "Create", "Contact", new { id = Model.person_id}, null)
</p>

@Html.Partial("ContactList", Model.contacts)
@Html.ActionLink("Back to Client List", "index", "Home")